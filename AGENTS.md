# AGENTS.md — post-card-memory 공용 작업 규칙

이 문서는 ChatGPT/Codex와 Claude Code가 함께 지키는 프로젝트 공통 규칙이다. 특정 에이전트의 도구 사용법과 세션 명령은 각 에이전트 전용 문서(`CLAUDE.md` 등)에만 둔다.

## 1. 기준 문서와 우선순위

지시와 의도의 우선순위는 다음과 같다.

```text
사용자의 현재 직접 지시 또는 이번 세션의 승인된 수동 작업지시서
→ 공용 작업판 모드의 docs/ai/CURRENT_TASK.md
→ docs/ai/DECISIONS.md
→ AGENTS.md
```

현재 브랜치, HEAD, 작업트리, 파일 상태와 호출 관계는 실제 저장소와 코드를 최종 근거로 삼는다. 문서와 실제 상태가 다르면 임의로 맞추지 말고 차이를 보고한다.

위 우선순위는 목표·의도를 해석하는 기준이지 안전선의 묵시적 해제가 아니다. 위험 수단은 5절, 승인·STOP 범위는 6절을 따른다. 과거 기록과 다음 작업 후보, 도구의 실행 허용 설정은 현재 작업의 승인 근거가 아니다.

문서별 역할:

- `CURRENT_TASK.md`: 공용 작업판 활성 시 승인된 목표 하나, 범위, 비범위, 완료 조건
- `WORK_CONTEXT.md`: 작업 중 새로 확인한 사실, 결정, 보류점과 사용자 판단 항목
- `STATUS.md`: 비개발자인 사용자가 지금 상황과 필요한 행동을 빠르게 보는 진행판
- `HANDOFF.md`: 현재 확인된 상태·미검증·위험과 다음 출발점. 완료 이력은 `docs/ai/archive/`에 보존하고 링크한다.
- `DECISIONS.md`: 여러 작업에 계속 영향을 주는 제품·구조 결정과 이유
- `AGENTS.md`: 모든 에이전트에 반복 적용되는 공통 안전 규칙
- `CLAUDE.md`: Claude Code에서만 필요한 실행·세션 규칙

일회성 오류, 특정 날짜의 임시 상태, 현재 작업 진행률은 공용 규칙이나 결정 기록에 넣지 않는다. `STATUS.md`는 현재 상황을 보여줄 뿐 새 작업을 지시하거나 기존 지시의 우선순위를 바꾸지 않는다.

**기존 수동 방식과의 대응 관계**: 이 구조를 도입하기 전부터 써온 "작업 지시서"와 "인수인계서"는 폐지되지 않는다. 작업 지시서는 공용 모드의 `CURRENT_TASK.md`(또는 수동 모드에서 세션에 직접 제공되는 지시서)에 대응하고, 인수인계서는 `HANDOFF.md`(상세 결과·검증·Git 상태)와 `STATUS.md`(사용자용 짧은 현황판)의 조합에 대응한다. `WORK_CONTEXT.md`와 `DECISIONS.md`는 기존 방식에 없던 보조 기록이며, 없어도 작업 진행을 막지 않는다.

## 2. 운영 모드

프로젝트는 다음 두 모드를 모두 공식적으로 지원한다.

### 수동 표준 모드 — 기본값

사용자가 다른 모드를 명시적으로 요청하지 않으면 항상 이 방식으로 시작한다.

```text
ChatGPT가 상세 작업지시서 작성
→ 사용자가 Claude Code에 전체 복붙
→ Claude Code 조사·구현·검증과 상세 완료보고
→ 사용자가 완료보고를 ChatGPT에 복붙
→ ChatGPT가 결과 검토와 인수인계 작성
```

- 사용자의 일반적인 기능 제안이나 "이 기능 해보자", "오늘 이거 해보자" 같은 말은 이 수동 모드에서 기능을 논의하고 Claude Code용 상세 작업지시서를 준비하라는 뜻으로 해석한다. 앱 코드를 바로 수정해도 된다는 승인이나 공용 작업판 모드 활성화 요청이 아니다.
- `CURRENT_TASK.md`, `WORK_CONTEXT.md`, `STATUS.md`, `HANDOFF.md` 갱신은 이 모드에서 필수 절차가 아니다. 갱신되지 않았다는 이유로 작업 시작·종료나 Claude Code 실행을 막지 않는다.
- 상세 작업지시서, 상세 완료보고서와 일일 인수인계서는 계속 유효한 공식 산출물이다. 사용자가 Codex 등 현재 작업자에게 직접 구현을 요청했다면 그 승인 범위에서 실행할 수 있다.
- 갱신 의무가 없다는 말은 재개 시 읽기 생략을 뜻하지 않는다. 이전 작업을 이으면 3절의 현재 상태·위험 확인을 수행한다.

### 공용 작업판 모드 — 명시적 선택사항

사용자가 "공용 작업판 모드로 하자", "CURRENT_TASK 방식으로 진행하자", "이번엔 자동 모드로 하자"처럼 명확하게 요청했을 때만 활성화한다.

```text
사용자와 ChatGPT가 방향 결정
→ CURRENT_TASK
→ Claude Code 조사·구현·검증
→ WORK_CONTEXT에 새 사실과 결정 기록
→ STATUS에 사용자용 진행 상황 표시
→ HANDOFF에 종료 결과 기록
```

- 공용 작업판 파일이 저장소에 존재한다는 사실만으로 이 모드가 활성화되지 않는다. 명시적 선택이 없으면 파일 내용과 관계없이 수동 표준 모드를 사용한다.
- 공용 작업판은 필요할 때 꺼내 쓰는 보조 도구와 공용 기억이지 프로젝트 실행의 필수 인프라가 아니다.
- 공용 문서나 특정 AI 도구를 사용할 수 없다는 이유만으로 프로젝트 작업 전체를 금지하지 않는다.
- `CURRENT_TASK.md`나 `HANDOFF.md`가 없거나 갱신할 수 없어도, 사용자가 제공한 완전한 수동 작업지시서가 있으면 그 범위로 진행할 수 있다.
- 작업판 활성 조건은 사용자의 명시적 선택과 현재 승인 범위·기록의 식별 가능성이다. 에이전트의 task-tracking UI 제공 여부와 분리한다. UI 부재를 숨기지 않되 그 이유만으로 작업판 모드를 중단하거나 전환하지 않는다. 실제 공용 기록을 사용할 수 없어 모드 변경이 필요하면 사용자에게 확인한다.
- 어느 모드에서 시작했든 사용자가 명시적으로 요청하면 현재 기록을 정리해 다른 모드로 전환한다. 더 적합해 보인다는 이유로 임의 전환하지 않는다.
- 특정 ChatGPT, Codex, Claude 세션 기록에만 프로젝트 핵심 기억을 맡기지 않는다.

## 3. 작업 시작

코드나 문서를 수정하기 전에 다음을 확인한다.

1. 현재 브랜치와 원격 추적 상태
2. 작업트리의 미커밋 변경
3. 최근 관련 커밋
4. 수동 표준 모드(기본값)이면 이번 세션에 제공된 승인된 상세 작업지시서
5. 사용자가 공용 작업판 모드를 명시적으로 선택했으면 `CURRENT_TASK.md`의 목표·범위·완료 조건
6. 공용 작업판 모드일 때 `WORK_CONTEXT.md`의 새 사실·결정·보류점
7. 공용 작업판 모드일 때 `STATUS.md`의 현재 단계와 사용자 확인 대기 항목
8. 이전 작업을 잇는 경우 모드와 무관하게 최근 완료보고 또는 최신 `HANDOFF.md`의 현재 상태·미검증·남은 위험을 확인한다. 접근 가능한 현재 기록을 읽고, 부족한 재개 조건은 명시한다. archive 전체를 매번 읽지 않고 관련 이력만 찾아본다
9. 관련 결정이 있으면 `DECISIONS.md`

기존 미커밋 변경은 사용자 작업으로 보고 덮어쓰거나 되돌리지 않는다. 현재 작업과 겹치면 수정 전에 충돌 가능성을 보고한다.

## 4. 기본 작업 방식

```text
조사
→ 반례 탐색
→ 재현 또는 근거 확보
→ (공용 모드) WORK_CONTEXT 갱신
→ 최소 수정
→ 가능한 검증
→ 전체 diff 재검토
→ (공용 모드) STATUS 갱신
→ (공용 모드) HANDOFF 갱신
→ 사용자 확인
```

- 한 번의 작업에는 핵심 목표를 하나만 둔다.
- 감사·리뷰·조사의 기본값은 읽기 전용이다. 구현·수정이 직접 요청되었거나 승인된 작업지시서에 명확하게 포함된 경우에만 수정한다. 결함 발견, 기능 논의, HANDOFF의 다음 후보는 구현 승인이 아니다.
- 확정되지 않은 결함을 추측으로 수정하지 않는다.
- 간접 참조, 과거 데이터 호환성, 비동기 실행 순서처럼 현재 판단이 틀릴 수 있는 반례를 확인한다.
- 현재 목표에 필요한 최소 변경만 수행한다.
- 요청하지 않은 리팩터링, 이름 변경, 포맷 정리, 파일 이동을 섞지 않는다.
- 범위 밖 문제는 수정하지 않고 기록만 남긴다(공용 모드는 `HANDOFF.md`의 발견 사항, 수동 모드는 완료보고서).
- 구현 승인 범위에서는 직접 연결된 호출 경로·테스트와 기존 dependency/API의 현재 버전 사용을 최소 범위에서 수정할 수 있다. 새 dependency 추가, 버전 변경, Gradle/plugin/build system 변경은 현재 승인에 명시적으로 포함되지 않았다면 관련 변경을 STOP한다.
- 공개 인터페이스·데이터 구조·저장 형식에 대한 현재 승인 범위를 넘어야 하면 먼저 보고한다.

## 5. 사용자 데이터와 앱 결과 보호

- 기존 엽서와 사용자 데이터 보존을 최우선으로 한다.
- Room Entity, 스키마, DAO 컬럼, Migration은 명시적인 범위와 충분한 조사 없이 변경하지 않는다.
- 새 저장이 성공하기 전에 기존 데이터나 파일을 삭제하지 않는다.
- 저장 실패, 취소, 경합 상황에서도 기존 데이터가 손상되지 않아야 한다.
- `CancellationException`은 일반 저장 오류와 구분한다.
- 화면 상태, Room 상태, 편집 미리보기, 저장·복원·공유·이미지 내보내기 결과의 일치를 유지한다.
- 현재 UI에서 사용하지 않는 필드나 경로도 과거 데이터 호환성과 삭제 방어 역할을 확인한다.
- 사용자 데이터 일괄 변환이나 삭제는 별도 승인 없이 수행하지 않는다.
- 실사용 실기기는 테스트 대상이 아니라 보호 대상이다. 명령어 이름이 아니라 효과 기준으로 판단한다: 앱의 설치 상태, package, 내부 저장소, Room DB, SharedPreferences, 앱 전용 파일에 영향을 줄 가능성이 있는 자동 검증은 실기기에서 사용자 명시 승인 없이 실행하지 않는다.
- 실기기에서 기본 금지: `connectedAndroidTest`, `connectedCheck`, instrumented test, 테스트 APK lifecycle을 발생시키는 task, uninstall, `pm uninstall`, `pm clear`, destructive migration, 데이터 초기화, package 제거 가능성이 있는 자동 task.
- 명령의 설치/제거 동작을 모르면 실행하지 않고 STOP한다. "검증을 위해 필요함"은 예외 사유가 아니다.
- 자동 계측 검증이 필요하면 emulator나 별도 테스트 환경을 사용한다. 실기기 검증은 사용자 수동 QA를 기본으로 하고, AI는 사용자가 명시적으로 승인한 일반 install/update, 일반 실행, READ-ONLY 상태 조회로만 관여한다.
- instrumentation 실행 전 `adb devices -l`로 연결 대상을 실측한다. 검증 전용 `emulator-*`만 있는 경우에만 자동 실행할 수 있다. 물리 기기나 정체를 확정할 수 없는 대상이 함께 보이면 자동 instrumentation을 실행하지 않고 사용자 확인을 받는다.
- `drawWithContent`, `graphicsLayer`, 화면 캡처나 렌더링 완료 대기가 필요한 emulator 테스트는 필요 시 `adb shell dumpsys power`로 `mWakefulness`를 확인한다. 화면이 `Asleep`이면 실제 draw pass가 없어 앱 timeout처럼 보일 수 있으므로, 검증 전용 emulator 화면만 깨운 뒤 재현 여부를 확인하고 이를 production 결함과 구분한다.
- 삭제·초기화·파일 정리 검증은 Fake, temporary directory, in-memory DB와 검증 전용 emulator를 우선한다. 실사용 기기의 DB·엽서·사진·`filesDir` 조작, `pm clear`, uninstall은 검증 수단으로 사용하지 않는다.
- 삭제 오케스트레이션은 `DB 삭제 성공 → 사용자 소유 파일 정리` 순서를 지킨다. DB 삭제 실패·예외 시 사용자 파일 삭제는 0건이어야 한다. `파일 삭제 + DB 행 잔존`은 금지해야 할 깨진 상태이고, DB 삭제 뒤 프로세스 종료 등으로 생길 수 있는 `DB 행 없음 + 파일 잔존`은 더 위험한 반대 상태를 피하기 위해 수용한 고아 파일 위험으로 구분한다. 고아 파일은 읽기 전용 `OrphanFileDiagnostics`로 진단하며, 진단 결과가 자동 삭제 승인을 뜻하지 않는다.

### 공통 효과 기반 실행 안전 원칙

- 코드 수정 여부나 명령 이름과 관계없이 실행 전 실제 대상과 읽기·쓰기·설치·제거·덮어쓰기·외부 전송 효과를 확인한다. 하위 task, hook, cleanup/teardown, 실패·취소 후 남는 상태까지 전체 lifecycle을 고려한다. 실기기뿐 아니라 개발 PC, Git working tree/worktree와 공유 저장소, credentials, 백업, emulator, 외부 저장소에도 적용한다.
- 목표 승인과 새로운 위험 수단 승인은 별개다. 기능 구현·검증·정리·복구·장기 자율 진행 승인을 사용자 데이터 변경, package 제거, 환경 초기화, history 변경, backup/restore의 포괄적 승인으로 해석하지 않는다. 일반적인 진행 지시나 과거 성공 선례는 구체적인 보호 규칙을 묵시적으로 해제하지 않는다.
- 사용자 소유 상태를 바꿀 가능성이 있는데 전체 효과를 확인할 수 없거나 새로운 위험 경계를 넘어야 하면 해당 실행을 STOP한다. 대상·경로·기기·계정이 불명확한 경우에도 추측으로 실행하지 않는다. 위험 수단의 승인은 대상, 영향, 실패 후 상태, 되돌릴 수 있는 범위를 설명한 뒤 받는다.
- 승인된 범위의 읽기 전용 조사·최소 수정과 효과가 확인된 검증은 반복 승인 없이 자율 진행한다. 같은 대상·수단·효과·조건에서는 긴 체크리스트를 반복하지 않는다. 대상, 옵션, 도구 버전, 설정, 권한이나 관찰 상태가 달라져 위험이 커지면 다시 판단한다. 통상적인 빌드 산출물 생성은 승인된 검증의 효과로 구분하되, 9절의 임의 정리·삭제·수동 편집 금지를 해제하지 않는다.
- emulator, clone, worktree, 임시 폴더라는 이름만으로 폐기 가능한 환경이라고 판단하지 않는다. 사용자 데이터와 공유 자원 연결 및 허용된 변경 범위를 확인한다. 병렬 작업도 공유 대상에 대한 효과와 충돌 가능성을 함께 판단한다.
- 백업이 필요하면 범위·시점·일관성·읽기 가능성과 별도 환경에서의 복원 검증 여부를 확인한다. 백업 설정이나 파일 존재만으로 복구를 보장하지 않는다. 백업이 있어도 위험 수단의 승인을 대신하지 않으며, 코드의 안전 커밋은 사용자 데이터의 백업 지점이 아니다.
- 데이터 손실, 예상 밖 설치 제거, migration 실패, 저장소 이상 등 중대한 사고가 발생하면 영향받는 상태에 대한 추가 쓰기·재시도·자동 복구를 중단한다. 상태를 보존하고 READ-ONLY 조사로 피해 범위·부분 변경·복구 후보를 확인한다. 원본이나 유일한 백업을 덮어쓰지 않으며, 복구는 구체적인 계획과 별도 승인 후 진행한다. 독립적인 안전한 읽기 전용 분석은 계속할 수 있다.
- 테스트 성공과 실행 후 사용자 환경 보존은 별도로 판단한다. 실패했다고 실행 전 상태가 유지됐다고 가정하지 않는다. 보호 경계 때문에 검증을 생략하면 대체 검증, 미확인 범위와 이유를 보고하며 완료 조건을 맞추려고 위험 경계를 넘지 않는다.

## 6. 범위 확대와 과주행 방지

아래 자율 진행과 선례 활용은 5절의 공통 효과 기반 실행 안전 원칙 안에서만 적용한다.

중대한 중단 사유가 없다면 조사, 코드 수정, 자동 검증, diff 검토는 이어서 진행한다. 중간 보고는 현재 위치를 알리기 위한 것이며, 단순히 보고했다는 이유로 사용자 응답을 기다리며 멈추지 않는다. commit과 push는 이 진행 원칙과 별개이며, 항상 사용자의 명시적 승인 또는 요청 후에만 수행한다.

### 승인과 STOP 범위

STOP은 현재 승인에 포함되지 않은 새로운 위험·범위·제품 판단이 발견됐을 때 적용한다. 대상·효과·조건까지 명시적으로 승인된 변경은 같은 이유로 재승인을 받지 않는다. 예를 들어 승인된 Room migration 자체는 재중단 사유가 아니지만, 예상 밖 데이터 삭제·schema 변경·데이터 의미 변화는 새 STOP 사유다. 5절의 실기기 보호와 효과 확인 의무는 그대로 유지한다.

- 위험 실행 STOP: 대상·효과가 불명확한 명령, 승인되지 않은 설치·삭제·복구 등 해당 실행을 멈춘다.
- 관련 수정 STOP: 사용자 변경과 충돌, 미승인 구조·dependency·제품 UX 변경, 해결책 역전 또는 아래 회차 제한에 도달하면 관련 production 수정을 멈춘다.
- 작업 전체 STOP: 안전하게 분리할 수 있는 승인된 작업이 없거나 사용자 지시로 전체 중단한 경우다. 단지 관련 수정이 멈췄다는 이유로 독립적이고 안전한 읽기 전용 조사·분석·보고까지 금지하지 않는다.
- STOP 보고에는 멈춘 대상, 근거, 계속 가능한 안전한 범위, 재개에 필요한 판단을 명시한다. 조사로 명백히 해결된 불확실성은 근거를 보고하고 원래 승인 범위 안에서 재개할 수 있다. 새 위험·제품 선택 승인이 필요한 경우 이를 조사 결과로 대신하지 않는다.

### 핵심 해결 시도 2회 제한

동일 원인 가설에 대한 핵심 해결 시도를 센다. 두 번째 시도 후에도 같은 핵심 문제가 해결되지 않으면 추가 production 수정을 멈추고 구조적 재검토·안전한 원인 분석·보고로 전환한다. 세 번째 시도는 근거와 새 계획을 보고하고 사용자 승인 후 진행한다.

단순 오타 보정, 실행환경·테스트 인프라 실패는 핵심 해결 시도와 분리해 기록한다. 문제나 가설 이름만 바꿔 횟수를 초기화하지 않는다. 새 해결책이 직전 해결책을 되돌리기 시작하면 횟수와 무관하게 관련 수정을 STOP한다.

시각적·감성적·실험적 기능은 가능한 경우 `최소 정적 형태 → 사용자 승인 → 핵심 동작 → 사용자 승인 → 저장·통합` 순서로 진행한다. 정적 형태가 승인되지 않았다면 애니메이션, DB, Migration, 복수 스타일로 확대하지 않는다. 이 단계별 승인은 해당 꾸미기 영역에서 새 UI/상호작용 문법을 처음 확정할 때 적용한다. 이미 이 절차로 확정되고 실기기 검증까지 끝난 문법을 같은 영역 안에서 반복 적용하는 작업은, 아래 자율 진행 조건을 만족하면 매번 처음부터 반복하지 않아도 된다. 문법을 다른 꾸미기 영역으로 그대로 옮기는 것은 새로 확정하는 일로 보고 이 절차와 아래 선례 사용 원칙을 따른다.

다음 조건을 모두 충족하는 작은 작업은 조사·구현마다 승인을 요청하지 않고 `조사 → 최소 구현 → 검증 → 결과 보고`를 한 단위로 진행할 수 있다.

- 같은 영역에서 이미 확정되고 실기기로 검증된 제품 문법을 그대로 반복 적용한다.
- 같은 역할을 하는 공용 component나 진입점이 이미 존재한다.
- 기능의 의미나 사용자 동작 방식이 달라지지 않는다.
- 저장 방식, 데이터 구조, Room/Migration에 영향이 없다.
- 더 안전한 대안이 따로 있을 여지가 없을 만큼 구현 방법이 명백하다.
- 영향 범위가 해당 작업이 속한 화면·컴포넌트로 제한된다.
- 자동 테스트 또는 실기기로 결과를 확인할 수 있다.

이 조건은 선례만으로 추가 승인을 생략할 수 있는 범위를 정한 것이다. 미충족 자체가 이미 명시적으로 승인된 작업의 재승인 사유는 아니다. 현재 승인 범위와 위 STOP·단계별 승인 기준으로 판단한다. 다음 변경은 선례만으로 승인되지 않으며 현재 승인에 포함되지 않았다면 STOP한다: 새로운 제품 문법의 최초 확정, 데이터 구조·저장 방식 변경, 대규모 공용 component 개편, 다른 꾸미기 영역으로의 문법 자동 전파, 기능 의미가 달라지는 UX 변경.

내부에서 이미 성공한 제품 문법은 판단 근거로 쓰되 자동으로 복사하지 않는다. 새 기능이 기존 선례와 실제로 같은 역할인지 먼저 확인해, 같으면 그 문법을 우선 활용하고 다르면 억지로 통일하지 않는다. 판단이 갈리면 외부 UI/UX 레퍼런스를 참고하고, 그래도 방향이 서지 않으면 아래 제품 판단 STOP을 따른다.

중요한 사용자 판단이 필요하면 관련 작업을 중단하고 다음을 함께 제공한다.

- 사용자에게 생긴 문제 또는 결정이 필요한 이유
- 조사해서 확인한 사실
- 가능한 선택지와 각각의 영향
- 작업자의 권장안과 그 이유
- 승인 전 멈춰 있는 작업

수동 표준 모드에서는 상세 보고에 기록한다. 공용 작업판 모드에서는 `WORK_CONTEXT.md`에 기록하고 `STATUS.md`에는 사용자가 이해할 수 있는 짧은 판단 대기 상태로 표시한다.

### 하위 agent와 Agent/Subagent/fork 위임 경계

- 메인 에이전트에게 부여된 자율 진행·수정 권한은 하위 agent나 Agent/Subagent/fork에 자동 상속되지 않는다. 하위 agent는 호출할 때 명시된 조사 대상, 허용 범위, production 수정 가능 여부, 종료 조건을 기준으로 움직인다.
- 조사 권한은 수정 권한을 포함하지 않는다. read-only 조사로 호출된 agent는 production code와 운영 문서를 수정하거나 commit·push하지 않는다.
- 한 차수나 작업의 위임은 다음 차수·다음 backlog 진입 권한을 포함하지 않는다. 범위 밖 문제를 발견하면 위치, 근거, 위험 가능성, 후속 후보만 반환하고 종료한다.
- 상속된 전체 대화나 작업지시서보다 현재 호출의 좁은 위임 범위가 우선한다. 상위 계획에 더 넓은 자율 진행 권한이 있어도 현재 Agent/Subagent/fork의 권한으로 해석하지 않는다.
- Agent/Subagent/fork는 여러 파일 reference 조사, 호출 경로·lifecycle 추적, dead runtime 전수조사, 기존 안전 선례와 영향 범위 조사처럼 독립적이고 경계가 분명한 read-only 조사에 우선 활용한다. 단순 검색 몇 번으로 끝나는 작업에는 억지로 사용하지 않는다.
- 하위 agent는 별도 허가 없이 또 다른 agent를 연쇄 호출해 범위를 넓히지 않는다.
- 메인 에이전트는 중요한 위임 결과를 필요한 수준에서 직접 재검증한다. 하위 agent가 코드를 수정했다면 결과를 채택하기 전에 diff, 핵심 reference, build와 필요한 test를 직접 확인한다.
- 하위 agent의 범위 이탈 결과가 우연히 안전해도 그 행동을 허용된 성공 선례로 취급하지 않는다. 결과의 기술적 안전성과 위임 절차 준수 여부를 분리해 기록한다.

## 7. 작업 맥락 기록

`docs/ai/WORK_CONTEXT.md`는 사용자가 공용 작업판 모드를 명시적으로 선택했을 때, 작업 도중 ChatGPT나 다음 세션이 판단 맥락을 복구하기 위한 선택적 공용 기록이다.

- 오늘 결정한 방향
- 조사해서 새로 확인한 사실
- 기존 예상과 달랐던 점
- 이미 내린 중요한 결정과 이유
- 아직 결정하지 않은 부분
- 범위를 넘을 가능성이 있는 사항
- 사용자 또는 ChatGPT 판단이 필요한 사항

원시 명령 출력이나 파일별 작업 로그를 쌓지 않는다. 중요한 사실이나 결정이 생겼을 때만 갱신한다. 해결된 보류점은 결과와 함께 정리하고, 상세 최종 검증과 Git 상태는 `HANDOFF.md`에 남긴다.

`CURRENT_TASK`, `WORK_CONTEXT`, `STATUS`, `HANDOFF`를 읽으면 기존 형식의 상세 작업지시서와 일일 인수인계서를 다시 작성할 수 있어야 한다. 일일 인수인계서는 프로젝트 역사와 회고를 위한 공식 기록으로 계속 작성할 수 있다.

## 8. 검증과 보고

- 검증 수단과 실행 환경은 5절의 공통 효과 기반 실행 안전 원칙을 먼저 충족해야 한다.
- 변경 영향에 맞는 검증부터 수행한다. 저장·직렬화·schema·공용 renderer·광범위 호출 경로가 바뀌면 검증 범위를 확대한다. 작은 국소 변경에 무조건 전체 테스트를 반복하거나 새 근거 없이 같은 검증을 재실행하지 않는다. 문서-only 작업은 검색·diff·링크 검증으로 충분할 수 있다.
- 구조 테스트와 실제 동작 검증이 보장하는 범위를 구분하고, 결함 재발 시 테스트가 실제 실패하는지 본다. 테스트 개수 증가는 목표가 아니며 개수 감소 자체를 회귀로 판정하지 않는다.
- 실행하지 못한 검증은 성공으로 표현하지 않고 이유와 남은 위험을 기록한다.
- 테스트를 통과시키려고 assertion이나 검증 조건을 약화하지 않는다.
- 비동기·경합 테스트는 고정 `delay()`보다 완료 순서를 결정적으로 통제하는 방식을 우선한다.
- 구현 코드뿐 아니라 테스트 입력, Fake, gate 위치와 dispatcher 설정도 오류 후보로 검토한다.
- 코드 실패와 Gradle, 네트워크, 권한, JDK, 파일 잠금 같은 실행환경 실패를 구분한다.
- 검증 결과는 `로컬 자동검증`, `emulator instrumentation`, `GitHub Actions CI`, `실기기 감각 QA`로 분리하고 각 영역을 `실행`, `미실행`, `불필요`, `실행 불가` 중 하나로 기록한다. 한 영역의 성공을 다른 영역의 성공으로 확대하지 않는다.
- 테스트 실패는 최소한 `test infrastructure`, `emulator / OS environment`, `fixture`, `timing / race`, `DB / migration`, `assertion`, `production`으로 분류한 뒤 수정 대상을 정한다. stack trace의 발생 위치와 timeout 값, production 코드의 timeout 값을 대조해 테스트 자체가 본문이나 assertion에 진입했는지도 확인한다.
- Compose BOM·Compose UI test·`androidx.test`·Espresso처럼 함께 동작하는 테스트 dependency 일부를 변경할 때는 현재 해석된 dependency, 최신 안정 버전, 대상 OS/API 호환성, 실제 resolved classpath를 함께 확인한다. 특정 버전 숫자를 영구 규칙으로 고정하지 않는다.
- 실제 Room 동작을 유지하면서 특정 DAO 실패만 재현해야 하면 `Room.inMemoryDatabaseBuilder`와 `object : Dao by realDao { override ... throw ... }` 위임 패턴을 권장 후보로 검토한다. 모든 테스트에 강제하거나 production 구조를 테스트 편의 때문에 바꾸지 않는다.
- push가 승인되어 수행됐다면 현재 `.github/workflows/android-ci.yml`의 실제 step과 GitHub Actions 결과를 확인한다. 현재 workflow는 `testDebugUnitTest`, `assembleDebug`, `assembleDebugAndroidTest`를 실행하며 instrumentation을 실제 구동한다고 과장하지 않는다. CI 실패는 YAML, Java, Gradle, SDK, runner, test infrastructure, production으로 먼저 분류하고 `test skip`, `continue-on-error`, 실패 step 제거로 숨기지 않는다.
- 자동 테스트 결과와 사용자의 실기기 결과를 분리한다.
- 자동 테스트 통과를 실기기 만족이나 버그가 전혀 없다는 뜻으로 과장하지 않는다.
- commit 분리나 임시 편집을 수행했다면 최종 상태에서 전체 변경과 관련 테스트를 다시 확인한다.
### 실기기 QA 필요성

작업별로 `실기기 QA 필요`, `자동 검증으로 충분`, `문서/테스트-only로 실기기 불필요` 중 하나와 근거를 적는다. 테스트-only라도 실제 영향이 있으면 필요성을 다시 판단한다. 자동 검증으로 확인할 수 있는 결과를 사용자에게 다시 검사시키지 않는다.

QA가 필요하면 대상 build/commit, 준비 조건·조작 순서, 기대 결과·관찰점, 자동 검증으로 확인하지 못한 이유를 제시한다. 에이전트별 질문 방식은 해당 어댑터를 따른다. 자연적으로 수십 회 실행해야 하는 조건 등 즉시 재현이 어려운 항목은 이유와 재개 조건을 명시한 미검증으로 남기며 사용자 데이터 조작이나 억지 완료를 요구하지 않는다. 필수 QA를 임의 면제하지 않으며, 미검증을 허용한 작업 종료 범위도 명확히 기록한다.

### 독립 상태와 공통 보고

구현·자동 검증·사용자 QA·commit·push를 각각 기록한다. 구현은 완료/부분 완료/미착수, 자동 검증은 통과/실패/미실행(이유), 사용자 QA는 대기/완료/불필요(근거)/문제 발견으로 구분한다. commit과 push는 각각 미승인·미실행·완료(근거)를 구분한다. 내부 task 완료는 해당 단계의 종료일 뿐 전체 기능 완료나 사용자 QA 승인이 아니다.

보고 순서는 아래 뼈대를 공통으로 사용하고 작업 규모에 맞게 간결하게 쓴다.

1. 앱/프로젝트에 달라진 점과 기존 데이터에 대한 의미
2. 구현 상태와 완료 범위
3. 자동 검증 상태와 근거
4. 사용자 확인 필요성·QA 상태
5. 알려진 미검증·위험·STOP과 재개 조건
6. 변경 파일과 이유
7. Git 상태(commit·push 별도)
8. 다음 행동(후보와 승인된 실행을 구분)

## 9. Git 안전

작업 시작과 종료 시 브랜치, 작업트리, 최근 커밋과 전체 diff를 확인한다.

- 기존 사용자 변경을 덮어쓰지 않는다.
- 관련 파일만 명시적으로 stage하고 `git add .`, `git add -A`를 사용하지 않는다.
- 필요한 사용자 QA가 남아 있으면 commit하거나 push하지 않는다. 8절 기준으로 실기기 불필요인 작업까지 기기 확인을 요구하지 않는다. QA 승인은 Git 승인을 대신하지 않는다.
- 필요한 확인 뒤에도 commit과 push는 각각 명시적인 요청·승인 범위에서만 수행한다. 이미 승인된 동일 범위를 반복 승인받지 않되 조건 변화나 새 위험은 5·6절로 판단한다.
- merge, rebase, reset, force push, 브랜치 삭제는 별도 요청 없이 하지 않는다.
- `git reset --hard`, `git clean -fd`, `git checkout .`, `git checkout -- .`, `git restore .`를 임의로 사용하지 않는다.
- `.kotlin/`, `build/`, `.gradle/`, `.idea/` 등 로컬 빌드·IDE 산출물을 임의 수동 편집·정리·삭제·stage·commit하지 않는다. 승인된 검증에서 발생하는 정상 산출물 생성은 5절로 판단한다.
- commit을 분리했다면 staged 내용과 전체 작업트리를 대조해 필요한 파일이 빠지지 않았는지 확인한다.

## 10. 사용자용 진행판

`docs/ai/STATUS.md`는 공용 작업판 모드를 선택했을 때 사용하는, AI 작업지시서나 기술 로그가 아니라 비개발자인 사용자를 위한 계기판이다. 파일을 열었을 때 10~20초 안에 오늘 목표, 현재 단계, 완료·진행 중·남은 작업, 발견된 문제, 사용자 확인 필요 항목을 알 수 있어야 한다.

- 사용자에게 보이는 문제와 앱의 의미를 먼저 쓰고 기술명은 필요할 때만 괄호로 짧게 덧붙인다.
- 상태는 `완료`, `진행 중`, `대기`, `사용자 확인 필요`, `문제 발견`, `중단`처럼 명확하게 구분한다.
- 테스트하지 않은 항목이나 실기기에서 확인하지 않은 항목을 `완료`로 표시하지 않는다.
- 파일 하나를 읽거나 코드 한 줄을 바꿀 때마다 갱신하지 않는다.
- 작업 시작, 조사에서 구현으로 전환, 중요한 문제 발견, 구현에서 테스트로 전환, 사용자 확인 대기, 작업 종료 시점에 갱신한다.
- 예상보다 큰 저장 구조 변경, Migration, 대규모 리팩터링, 기존 기능이나 미커밋 변경과 충돌, 테스트 실패, 범위 밖 수정 필요는 마지막 인수인계까지 숨기지 않고 즉시 표시한다.
- 사용자가 직접 판단해야 할 실기기 결과, 제품 선택, 데이터 구조 변경 승인, commit 승인은 `사용자 확인 필요`에 별도로 적는다.
- `STATUS.md`에는 상세 기술 구현을 복제하지 않는다. 상세 근거와 Git·검증 정보는 `HANDOFF.md`에 남긴다.

## 11. 독립 작업 단위 종료와 Android Studio 작업 환경

- Android Studio와 CLI Gradle의 동시 실행으로 파일 잠금이 생길 수 있다. 같은 잠금 오류를 무한 재시도하지 않는다.
- 잠금이 반복되면 잠긴 대상, 첫 실제 오류, 재시도 횟수와 확인 가능한 관련 프로세스를 보고한다.

`HANDOFF.md` 갱신은 세션 종료 전용 규칙이 아니다. 공용 작업판 모드에서는 다음과 같은 독립 작업 단위 종료 시점마다 적용한다.

- 조사 완료
- 구현 + 자동 검증 완료
- 실기기 검증 완료
- commit / push 완료
- 중요한 제품 판단 확정
- 다음 독립 작업 진입 직전

반대로 다음과 같이 아직 판단이 끝나지 않은 중간 상태마다 반복 갱신하지 않는다.

- 파일 하나를 읽음
- 코드 몇 줄만 수정
- 중간 테스트 실행
- 판단이 끝나지 않은 상태

문서 관리가 본 작업을 방해하지 않게 한다.

기록의 역할은 다음과 같이 나뉜다.

- Git: 실제 코드 상태의 기준
- `AGENTS.md` / `CLAUDE.md`: 작업 규칙
- `docs/ai/HANDOFF.md`: 작업 맥락·제품 판단·현재 상태·다음 작업을 담는, Claude Code와 Codex 등 여러 작업 에이전트가 프로젝트를 교대할 때 쓰는 공용 교대 장부
- 현재 세션 기억: 임시 보조 맥락일 뿐, `HANDOFF.md`를 대체하지 않는다

`HANDOFF.md`를 코드 상태의 절대 진실 소스로 취급하지 않는다. 실제 코드 상태는 항상 Git이 기준이다. 에이전트별 세션 관리 조건은 해당 전용 문서를 따른다.

- 공용 작업판 모드에서는 위 종료 시점마다 `WORK_CONTEXT.md`의 중요한 내용을 정리하고 `STATUS.md`를 사용자가 다음 행동을 알 수 있는 상태로 갱신한 뒤, `HANDOFF.md`에 변경 파일, 성공한 검증, 미실행 검증, 다음 작업 하나를 남긴다.
- 수동 표준 모드에서는 상세 완료보고서와 일일 인수인계서가 같은 역할을 한다. 공용 파일을 갱신하지 않았다는 이유로 작업 단위 종료나 완료보고를 막지 않는다.
- `HANDOFF.md`에는 확인한 사실만 쓰고 미확인 항목은 `미확인` 또는 `사용자 확인 대기`로 표시한다.
- 공용 작업판 모드의 `CURRENT_TASK.md`는 새 승인 작업을 시작할 때 교체한다. 수동 모드의 비활성 작업판 파일은 현재 지시로 읽지 않는다.
- `HANDOFF.md`를 갱신할 때는 현재 사실·8절의 독립 상태·남은 위험·재개 조건·다음 후보를 앞에 둔다. 완료 이력은 `docs/ai/archive/`에 보존하고 날짜·주제 링크를 남긴다. 사고·실패·폐기 접근 원문을 삭제·축약하거나 현재 복구 사실과 섞지 않는다. archive는 역사 자료이지 현재 상태나 실행 승인이 아니다.

## 12. 대화 방식

- 사용자에게 한국어 반말로 친근하고 정확하게 설명한다.
- 기술 용어보다 사용자에게 보이는 문제, 바뀌는 앱 행동, 필요한 기술적 이유 순서로 설명한다.
- 코드와 테스트로 확인할 수 있는 내부 선택은 작업자가 조사해 결정한다.
- 제품 동작, 데이터 영향, 범위 확대, 비용·권한, 되돌리기 어려운 작업만 사용자에게 판단을 요청한다.

## 13. 확인된 프로젝트 불변값

- 갤러리의 기본 목록은 3열 그리드를 유지한다.
- 엽서에 표시·저장하는 기본 날짜 형식은 `yyyy-MM-dd`를 유지한다.
- 같은 꾸미기 요소의 화면 미리보기와 저장·공유 exporter는 가능한 한 공통 렌더러와 계산식을 사용한다.
- 화면과 exporter의 계산이 갈라지는 변경은 완료로 처리하지 않는다.
- 작은 기능은 작은 기능으로 끝내고 요청 없이 범용 시스템으로 확대하지 않는다.
- 기능 작업과 별도로 남겨둔 저장소 청소 작업은 같은 변경에 섞지 않는다.
