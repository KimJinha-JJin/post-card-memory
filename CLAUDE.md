# CLAUDE.md — post-card-memory 작업 규칙

이 문서는 `post-card-memory`에서 반복 적용할 Claude Code의 공통 작업 원칙과 안전선을 정의한다.

현재 작업의 구체적인 목표와 범위는 작업 지시서에서, 이전 작업의 실제 결과와 출발점은 인수인계서에서 확인한다.

## 1. 문서 역할과 우선순위

* **작업 지시서**: 현재 작업의 목표, 범위, 비범위, 완료 조건을 정의한다.
* **인수인계서**: 이전 작업 결과와 현재 작업의 출발점을 전달한다.
* **CLAUDE.md**: 모든 작업에 반복 적용되는 공통 수행 방식과 안전 규칙을 정의한다.

지시와 의도의 우선순위는 다음과 같다.

```text
사용자의 현재 직접 지시
→ 현재 작업 지시서
→ 최신 인수인계서
→ CLAUDE.md
```

현재 브랜치, HEAD, 파일 상태, 호출 관계와 같은 사실관계는 실제 저장소와 코드를 최종 근거로 삼는다. 문서와 실제 상태가 다르면 임의로 맞추지 말고 차이를 보고한다.

## 2. 기본 작업 방식

기본 흐름은 다음과 같다.

```text
조사
→ 반례 탐색
→ 재현 또는 근거 확보
→ 최소 수정
→ 가능한 검증
→ 전체 diff 재검토
→ 보고
```

* 확정되지 않은 결함을 추측으로 수정하지 않는다.
* 간접 참조, 과거 데이터 호환성, 비동기 순서 등 현재 판단이 틀릴 가능성을 확인한다.
* 현재 목표를 달성하는 데 필요한 최소 변경만 수행한다.
* 요청하지 않은 리팩터링, 이름 변경, 포맷 정리, 파일 이동을 섞지 않는다.
* 범위 밖에서 발견한 문제는 수정하지 않고 별도로 기록한다.
* 현재 목표와 직접 연결된 호출 경로, 테스트, 의존성 변경은 최소 범위에서 함께 수정할 수 있다.
* 공개 인터페이스, 데이터 구조, 저장 형식 또는 작업 지시서에 없는 기능 영역까지 변경해야 한다면 먼저 보고하고 승인을 받는다.

## 3. Task 운영

규모가 있는 다단계 작업에서는 Claude Code 내장 Task 기능을 사용한다.

* 동시에 `in_progress`인 Task는 하나만 유지한다.
* 선행 Task가 완료되지 않았다면 후속 Task를 시작하지 않는다.
* 코드 수정만으로 Task를 완료 처리하지 않고, 가능한 검증과 diff 확인 후 완료한다.
* 조사 결과 기존 전제가 달라지면 Task의 범위와 완료 조건을 갱신한다.
* 자동 테스트를 실행할 수 없으면 Task를 무기한 막아두지 말고, 실행하지 못한 이유와 남은 검증을 기록한다.
* 코드 수정과 가능한 자동 검증이 끝나면 해당 Task는 완료 처리하되, 실기기 검증이 아직 이루어지지 않았다면 최종 보고에 `사용자 검증 대기` 항목으로 남겨 다음 작업에서도 추적되게 한다.

단순한 오탈자 수정이나 범위가 명확한 소규모 변경에는 불필요하게 Task를 만들지 않는다.

## 4. 진행과 승인 경계

* 조사, 코드 수정, 자동 검증, diff 검토는 중대한 중단 사유가 없다면 이어서 진행한다.
* 중간 보고는 현재 위치를 알리기 위한 것이며, 단순히 보고했다는 이유로 사용자 응답을 기다리며 멈추지 않는다.
* commit과 push는 위 진행 원칙과 별개이며, 항상 사용자의 명시적 승인 또는 요청 후에만 수행한다.

다음은 진행 여부와 별개로, 발견 즉시 범위를 확대하지 말고 보고해야 하는 상황이다.

* 사용자 데이터 손상 위험
* 기존 미커밋 변경과의 충돌
* Room 스키마 또는 Migration 변경 필요
* 현재 작업 범위를 넘는 구조 개편 필요
* 사용자가 선택해야 하는 실질적으로 다른 대안이 존재
* 같은 오류가 반복되어 추가 재시도가 무의미함

## 5. 데이터 안전

* 기존 사용자 데이터 보존을 최우선으로 한다.
* Room Entity, 스키마, DAO 컬럼, Migration은 명시적인 작업 범위와 충분한 조사 없이 변경하지 않는다.
* 현재 UI에서 사용하지 않는 필드나 파일 경로도 과거 데이터 호환성과 삭제 방어 역할을 확인한다.
* 새로운 저장이 성공하기 전에 기존 데이터나 파일을 삭제하지 않는다.
* 저장 실패, 취소, 경합 상황에서도 기존 데이터가 손상되지 않아야 한다.
* `CancellationException`은 일반 저장 오류와 구분한다.
* 화면 상태, Room 상태, 미리보기, 갤러리 저장 및 공유 결과의 일치를 유지한다.
* 사용자 데이터 일괄 변환은 별도 승인 없이 수행하지 않는다.

## 6. 테스트와 검증

* 실행 가능한 자동 테스트와 컴파일 검증을 수행한다.
* 실행할 수 없는 검증은 성공한 것처럼 표현하지 않고, 이유와 남은 검증을 보고한다.
* 테스트를 통과시키기 위해 assertion이나 검증 조건을 약화하지 않는다.
* 비동기 및 경합 테스트는 고정 `delay()`보다 완료 순서를 결정적으로 통제할 수 있는 방식을 우선한다.
* 구현 코드뿐 아니라 테스트 입력, Fake, gate 위치와 dispatcher 설정의 오류 가능성도 검토한다.
* commit 분리나 임시 편집을 수행했다면 최종 상태에서 전체 변경과 관련 테스트를 다시 확인한다.

## 7. Git 안전

작업 시작과 종료 시 현재 브랜치, 작업트리, 최근 커밋과 diff를 확인한다.

* 기존 사용자 변경을 덮어쓰지 않는다.
* 관련 파일만 명시적으로 stage한다.
* `git add .`와 `git add -A`를 사용하지 않는다.
* commit과 push는 사용자의 명시적 요청 후에만 수행한다.
* merge, rebase, reset, force push, 브랜치 삭제는 별도 요청 없이 하지 않는다.
* 다음 명령을 임의로 사용하지 않는다.

```bash
git reset --hard
git clean -fd
git checkout .
git checkout -- .
git restore .
```

* `.kotlin/`, `build/`, `.gradle/`, IDE 워크스페이스 파일 등 로컬 빌드·IDE 산출물을 수정, 삭제, stage, commit하지 않는다.
* commit을 분리한 뒤에는 staged 내용과 전체 작업트리를 대조하여 필요한 파일이 누락되지 않았는지 확인한다.

## 8. Android Studio와 CLI

* Android Studio와 CLI Gradle이 동시에 실행되면 빌드 산출물 파일 잠금이 발생할 수 있다.
* 동일한 잠금 오류를 무한 재시도하지 않는다.
* 반복 실패 시 잠금 대상, 실제 오류, 재시도 횟수와 가능하면 확인된 관련 프로세스를 보고한다.
* Android Studio 내부 터미널의 세션을 종료해야 한다면, 종료 전에 현재 진행 상태, 변경 파일, 성공한 검증, 실행하지 못한 검증과 다음 단계를 남긴다.

## 9. 보고

대부분의 작업에서는 다음 최소 보고 형식을 사용한다.

```text
변경 파일:
핵심 변경:
검증 방법과 결과:
남은 위험 또는 미검증 항목:
Git 상태: (변경 없으면 "이상 없음")
```

다음 경우에는 원인, 재현, 반례, 대안, 데이터 안전과 테스트 결과를 포함한 상세 보고를 작성한다.

* 여러 파일에 걸친 구조 변경
* 저장 경합 또는 데이터 손상 가능성이 있는 버그
* Room 스키마 또는 Migration 관련 작업
* 사용자가 상세 보고를 요청한 경우

작업 규모에 비해 불필요하게 긴 보고 양식을 강제하지 않는다.

## 10. CLAUDE.md 갱신

다음과 같이 사용자가 장기 규칙 저장을 명시한 경우에만 이 문서를 갱신한다.

* `CLAUDE.md에 반영해줘`
* `프로젝트 규칙으로 남겨줘`
* `앞으로 이 방식으로 작업해줘`
* `이 규칙을 기억해줘`
* 입력 맨 앞의 `#`을 이용한 메모리 추가 요청

갱신할 때는 다음을 따른다.

* 장기간 반복 적용할 규칙만 기록한다.
* 특정 날짜의 HEAD, 현재 Task 상태, 일회성 오류, 임시 파일명은 기록하지 않는다.
* 비밀번호, 토큰, API 키, 개인 계정명, 개발자 PC의 절대 경로, 로컬 포트 등 민감하거나 로컬 전용인 정보를 기록하지 않는다.
* 기존 규칙과 중복되면 새 항목을 누적하지 말고 기존 문장을 보완한다.
* 기존 규칙과 충돌하면 임의로 덮어쓰지 않고 사용자에게 알린다.
* 메모리 갱신만 요청받은 경우 코드 수정, 테스트, commit, push를 함께 수행하지 않는다.
