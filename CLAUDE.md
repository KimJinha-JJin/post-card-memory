@AGENTS.md

# CLAUDE.md — Claude Code 전용 어댑터 (post-card-memory)

공통 프로젝트 규칙과 안전선(사용자 데이터 보호, Room/Migration, Git 안전, 테스트와 검증, 범위 확대 방지, commit/push 승인 등)은 `AGENTS.md`를 따른다. 이 문서에는 Claude Code에서만 필요한 실행·세션 규칙을 둔다.

## 시작 순서

1. `AGENTS.md`를 읽고 사용자가 공용 작업판 모드를 명시적으로 선택했는지 확인한다.
2. 명시적 선택이 없으면 수동 표준 모드(기본값)로 보고 이번 세션에 제공된 승인된 상세 작업지시서를 기준으로 삼는다.
3. 공용 작업판 모드이면 `docs/ai/CURRENT_TASK.md`를 읽는다.
4. 공용 작업판 모드일 때만 `WORK_CONTEXT.md`, `STATUS.md`, `HANDOFF.md`에서 새 사실·현재 단계·마지막 검증을 읽는다.
5. 현재 작업에 관련된 항목이 있을 때만 `docs/ai/DECISIONS.md`를 읽는다.
6. 실제 브랜치, HEAD, 작업트리와 기록이 다르면 수정 전에 차이를 보고한다.

## 내장 Task 운영

규모가 있는 다단계 작업에서는 Claude Code 내장 Task 기능을 사용한다. 단순한 오탈자 수정이나 범위가 명확한 소규모 변경에는 불필요하게 Task를 만들지 않는다.

- 동시에 `in_progress`인 Task는 하나만 유지한다.
- 선행 Task가 완료되지 않았다면 후속 Task를 시작하지 않는다.
- 코드 수정만으로 Task를 완료 처리하지 않고, 가능한 검증과 diff 확인 후 완료한다.
- 조사 결과 기존 전제가 달라지면 Task의 범위와 완료 조건을 실제 상태에 맞게 갱신한다.
- 자동 테스트를 실행할 수 없으면 Task를 무기한 막아두지 말고, 실행하지 못한 이유와 남은 검증을 기록한다.
- 코드 수정과 가능한 자동 검증이 끝나면 해당 Task는 완료 처리하되, 실기기 검증이 아직 이루어지지 않았다면 최종 보고에 `사용자 검증 대기` 항목으로 남긴다. 공용 작업판 모드에서는 같은 내용을 `HANDOFF.md`에도 반영해 다음 작업에서도 추적되게 한다.

내장 Task 기능을 사용할 수 없으면 자동 모드인 척 텍스트 체크리스트로 몰래 대체하지 않는다. 사용할 수 없는 기능과 현재 상태를 보고한다. 공용 작업판 모드를 사용 중이었다면 이를 중단하고 사용자에게 알린다. 승인된 상세 작업지시서가 있는 수동 표준 모드에서는 공용 작업판 파일의 상태와 관계없이 그 지시서를 기준으로 계속 진행할 수 있다.

## 보고 형식

Claude Code가 사용자에게 직접 답할 때는 다음 최소 보고 형식을 사용한다. 수동 표준 모드에서는 이 형식이 완료보고서 역할을 겸하고, 공용 작업판 모드에서는 같은 내용이 `HANDOFF.md`에도 반영된다.

```text
변경 파일:
핵심 변경:
검증 방법과 결과:
남은 위험 또는 미검증 항목:
Git 상태: (변경 없으면 "이상 없음")
```

다음 경우에는 원인, 재현, 반례, 대안, 데이터 안전과 테스트 결과를 포함한 상세 보고를 작성한다.

- 여러 파일에 걸친 구조 변경
- 저장 경합 또는 데이터 손상 가능성이 있는 버그
- Room 스키마 또는 Migration 관련 작업
- 사용자가 상세 보고를 요청한 경우

작업 규모에 비해 불필요하게 긴 보고 양식을 강제하지 않는다.

## 세션 관리

- `/compact`와 `/clear`는 Claude가 자동 실행하지 않고 사용자가 최종 결정한다.
- 조사, 구현, 실패 분석, 실기기 확인이 진행 중이거나 중요한 미커밋·미푸시 변경이 있으면 둘 다 제안하지 않는다.
- 대화가 길어졌다는 사실만으로는 `/compact`나 `/clear`를 권장하지 않는다. 판단 기준은 항상 작업 단위의 종료 여부다.
- 같은 흐름의 논리적인 작업 단위가 끝나고 `STATUS.md`와 `HANDOFF.md`가 최신이면 `/compact`를 검토할 수 있다. 이 조건을 만족하는 시점에는 "필요하면 compact 해도 됩니다" 같은 흐린 표현 대신 `COMPACT 권장 지점`을 명시적으로 출력하고, 왜 지금이 그 지점인지 이유를 한 줄 덧붙인다. 실행 여부는 사용자가 결정한다.
- `/clear`는 현재 작업이 완전히 끝났고 다음 작업이 무관하며, 새 세션이 `AGENTS.md`와 `docs/ai/`만으로 복구될 때만 검토한다. 이 조건을 만족하는 시점에는 `CLEAR 권장 지점`을 명시적으로 출력하고 이유를 한 줄 덧붙인다. 실행 여부는 사용자가 결정한다.
- 다음 상태에서는 `/clear`를 권장하지 않는다: production 수정 진행 중, 자동 테스트 실패, 실기기 검증 전, 미커밋 변경이 작업상 중요한 상태, 중요한 제품 판단이 미결정, STOP 이슈 미해결, 조사 결과와 실제 코드가 충돌하는 상태, 다음 작업이 현재 작업과 강하게 이어져 있는 상태.
- 사용자가 `/compact` 뒤 작업을 재개하면 브랜치, HEAD, 작업트리, 마지막 결과, 남은 위험과 다음 목표를 다시 확인한다.

## Compact Instructions

대화를 압축할 때 다음 내용을 반드시 보존한다. `/clear`는 대화 자체가 사라지므로 압축이 아니라 사전 조건으로 적용한다 — 아래 항목이 대화 안에만 있고 파일(`WORK_CONTEXT.md`, `STATUS.md`, `HANDOFF.md` 또는 상세 완료보고서)에 기록되어 있지 않다면 `CLEAR 권장 지점`을 출력하지 않는다.

- 현재 브랜치, HEAD, origin 동기화, 작업트리
- 완료한 범위와 완료 조건 판정
- 확인된 원인, 실제 해결 방법, 폐기한 접근과 이유
- 변경 파일과 실행한 검증의 실제 결과
- `WORK_CONTEXT.md`에 기록한 새 사실, 결정, 보류점
- 데이터·저장·복원·공유·내보내기 호환성
- 사용자 실기기 확인 상태와 승인 대기 항목
- 남은 위험, 다음 작업 하나, 건드리지 않을 범위

## 규칙 갱신

다음과 같이 사용자가 장기 규칙 저장을 명시한 경우에만 `AGENTS.md`나 이 문서를 갱신한다.

- `CLAUDE.md에 반영해줘` / `AGENTS.md에 반영해줘`
- `프로젝트 규칙으로 남겨줘`
- `앞으로 이 방식으로 작업해줘`
- `이 규칙을 기억해줘`
- 입력 맨 앞의 `#`을 이용한 메모리 추가 요청

갱신할 때는 다음을 따른다.

- 장기간 모든 에이전트가 지킬 규칙은 `AGENTS.md`에 반영한다.
- Claude Code의 도구나 세션에만 해당하는 규칙은 이 문서에 반영한다.
- 사용자가 공용 작업판 모드를 명시적으로 선택했을 때만 현재 목표는 `CURRENT_TASK.md`, 중간 판단 맥락은 `WORK_CONTEXT.md`, 사용자용 진행 상황은 `STATUS.md`, 실제 상세 결과는 `HANDOFF.md`, 장기 결정과 이유는 `DECISIONS.md`에 기록한다.
- 수동 표준 모드에서는 상세 작업지시서, 완료보고서와 일일 인수인계서를 기본 산출물로 사용하며 공용 문서 갱신을 강제하지 않는다. 이 산출물의 작성을 금지하거나 폐지하지 않는다.
- 장기간 반복 적용할 규칙만 기록한다. 특정 날짜의 HEAD, 현재 Task 상태, 일회성 오류, 임시 파일명은 기록하지 않는다.
- 비밀번호, 토큰, API 키, 개인 계정명, 개발자 PC의 절대 경로, 로컬 포트 등 민감하거나 로컬 전용인 정보를 기록하지 않는다.
- 기존 규칙과 중복되면 항목을 누적하지 말고 기존 문장을 보완한다.
- 기존 규칙과 충돌하면 임의로 덮어쓰지 않고 사용자에게 알린다.
- 메모리 갱신만 요청받았다면 코드 수정, 테스트, commit, push를 함께 수행하지 않는다.
